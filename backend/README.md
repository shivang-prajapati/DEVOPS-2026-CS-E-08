# Backend

This folder is owned by the backend team member.

Add the server application, API routes, data models, and environment configuration here. Keep secrets in environment variables and document required variables in `.env.example`.
