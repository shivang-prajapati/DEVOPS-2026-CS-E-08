# Testing

This folder is owned by the testing team member.

Add automated tests, test fixtures, and test documentation here. Keep tests independent from production code and update the root `Jenkinsfile` when new test commands are added.
